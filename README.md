# VESTIR — Styling Studio

2D flat-lay board: built-in garment SVGs, image cutouts, layers, and export.

Open `index.html` in a browser, or serve the folder locally for PNG export (`html2canvas` loads from a CDN).
